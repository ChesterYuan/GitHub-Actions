name: first-workflow

on: push

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: first-step
        run: echo "Hello World!"

      - name: second-step
        run: ls

      - name: third-step
        run: pwd

      - name: fourth-step
        run: cat README.md
